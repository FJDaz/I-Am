[build]
builder = "NIXPACKS"
buildCommand = "cd Backend && pip install -r requirements.txt"

[deploy]
startCommand = "cd Backend && python rag_assistant_server.py"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
